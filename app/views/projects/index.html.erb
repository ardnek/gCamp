
<div class="page-header">

  <div class="pull-right">
      <%= link_to "Create Project", new_project_path, :class => "btn btn-info btn-sm" %>
  </div>

<h3>My Projects</h3>

</div>


<table class="table table-condensed">
  <thead>
    <tr>
    <th width="40%">Name</th>
    <th width="30%">Members</th>
    <th width="25%">Tasks</th>
    </tr>

  </thead>

  <tbody>
    <% @projects.each do |project| %>
    <tr>
      <td><%= link_to project.name, project_path(project) %></td>
      <td><%= link_to (project.memberships.count), project_memberships_path(project) %></td>
      <td><%= link_to (project.tasks.count), project_tasks_path(project) %></td>
    </tr>
    <% end %>
  </tbody>
</table>


<div class="page-header">

  <h3>Pivotal Tracker Projects</h3>

</div>


<table class="table table-condensed">
  <thead>
    <tr>
      <th width="40%">Name</th>
      <th width="30%">Members</th>
      <th width="25%">Tasks</th>
    </tr>

  </thead>

  <tbody>
    <% @projects.each do |project| %>
    <tr>
      <td><%= link_to project.name, project_path(project) %></td>
      <td><%= link_to (project.memberships.count), project_memberships_path(project) %></td>
      <td><%= link_to (project.tasks.count), project_tasks_path(project) %></td>
    </tr>
    <% end %>
  </tbody>
</table>
