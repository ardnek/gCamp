<div class="container">
<div class="page-header">

<%= link_to "Create Project", new_project_path, :class => "btn btn-info btn-sm pull-right" %>

<h3>Projects</h3>



<table class="table table-condensed">
  <thead>
    <tr>
    <th>Name</th>
    </tr>

  </thead>

  <tbody>
    <% @projects.each do |project| %>
    <tr>
      <td><%= link_to project.name, project_path(project) %></td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
