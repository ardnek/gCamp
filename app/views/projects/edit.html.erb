<div class="container">
  <h3>Edit Project</h3>
<%= form_for(@project) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.submit "Edit Project", :class => "btn btn-info btn-sm" %>

<% end %>
</div>
