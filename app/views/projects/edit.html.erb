
  <h3>Edit Project</h3>
<%= form_for(@project) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.submit "Edit Project", :class => "btn btn-info btn-sm" %>
<%= link_to "Cancel", project_path, class: 'btn btn-default btn-sm' %>

<% end %>
